---
description: Custom Data Toolkit 项目级 AI Coding 硬约束
globs:
alwaysApply: true
---

# Custom Data Toolkit Rules

## 必读

编码前阅读 `docs/product.md`、`docs/architecture.md`、`docs/data-contract.md`、`docs/api.md`、`docs/development.md` 与 `AGENTS.md`。
行为权威优先：`openspec/specs/{auth,currency,rate,customs-dict}/`；活跃 change 见 `openspec/specs/README.md`；历史见 `openspec/changes/archive/`。
文档权威与更新矩阵见 `docs/README.md`。

## MUST

- 每个切片严格：`plan → implement → verify → test → review → document`；**Plan 须用户确认后才能写业务代码**
- 管理端鉴权：Session Cookie + CSRF；对外查询：`X-API-Key`
- 复用 MySQL 表 `currency`、`rate`，不要擅自改列语义（`data` 为字符串汇率）
- 后端分层：routers → services → repositories → models
- 行为/API/外部表变更写回权威层（OpenSpec / `api.md` 或未来 OpenAPI / `data-contract.md`）
- Verify 必须对照 OpenSpec Scenario / `development.md` Checklist 逐条；Test 必须实跑

## MUST NOT

- 跳过 Plan 门禁或自说自话扩大范围
- 实现已搁置的整表覆盖全量/操作日志/处理历史；字典 Redis 禁止擅自整表覆盖
- 添加自助注册或 Casdoor/Casbin（MVP）
- 提交 `.env`、密码或 API Key 明文
- 在对外 `/public/*` 提供写接口
- 用 PostgreSQL 替换已决定的 MySQL 复用方案（除非新增 ADR）
- 以“代码看起来正确”代替 Verify/Test
- 恢复或新建与权威文档双写的停用文件（见 `docs/archive/README.md`）

## 验证

完成后对照 `docs/development.md` 验收清单与相关 OpenSpec Verification Checklist；实际跑测试，不以“看起来对”代替。
