# custom-data-toolkit

FastAPI service generated for Tendata backend runtime `python`.

```bash
uv sync --group dev --group test
make test
make run
curl http://localhost:8000/health
```

Application package: `custom_data_toolkit`.
