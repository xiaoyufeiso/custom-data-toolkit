# Tasks: 管理页面内批量删除与 API Key 筛选

> 按用户可验收结果拆分；每个切片严格执行 plan → implement → verify → test → review → document。

## 1. 批量删除公共契约

- [x] 1.1 定义统一 ID 请求、可选错误 details、批次上限和错误码
- [x] 1.2 建立批量查询、缺失 ID 校验、单次提交和回滚测试基线
- [ ] 1.3 验证三个端点均要求 Session + CSRF，单条删除保持兼容

## 2. 货币当前页批量删除

- [x] 2.1 管理员可在筛选后的当前页点选或全选货币
- [x] 2.2 页面顶部显示带计数的删除按钮，操作列移除删除
- [x] 2.3 批量检查关联汇率；任一阻塞或缺失时整批回滚并提示 IDs
- [x] 2.4 覆盖选择清理、确认、成功、HasRates、过期选择和页码回退

## 3. 汇率当前页批量删除

- [x] 3.1 管理员可在筛选后的当前页点选或全选汇率
- [x] 3.2 页面顶部删除经确认后原子删除，操作列只保留编辑
- [x] 3.3 覆盖选择清理、成功、过期选择、回滚和页码回退

## 4. API Key 筛选与批量删除

> **Deferred（2026-07-31）**：前端 API Key 模块已整体移除并搁置；本节与对应 `specs/api-key` 暂不实施。后端公开查询鉴权仍保留。

- [ ] 4.1 API Key 列表支持名称包含和启用状态组合筛选
- [ ] 4.2 结合 `standardize-admin-ui-components` 切片 4 使用 BizTable 和 tendata-ui 筛选控件
- [ ] 4.3 当前页点选/全选后可在页面顶部原子批量删除
- [ ] 4.4 保持启停行为和明文 Key 仅创建时展示一次
- [ ] 4.5 覆盖筛选、选择清理、确认、立即失效、过期选择和页码回退

## 5. 完整验收与文档

- [x] 5.1 对照 currency/rate/ui 已实现 Scenario 验收（api-key UI 豁免）
- [x] 5.2 实跑后端测试、前端测试、lint、类型检查和 UAT 构建（M6 2026-07-31）
- [x] 5.3 确认无数据库迁移、无公开写接口、无鉴权边界变化
- [x] 5.4 更新 `docs/api.md`、本 tasks 和必要进度文档
- [x] 5.5 归档：currency/rate delta 已合并进领域 spec；api-key §4 仍 deferred（无前端模块）

## TBD / Approval

无。实现开始前仍需逐切片提交 Plan 并获得用户确认。
