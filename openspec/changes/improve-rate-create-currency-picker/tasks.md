# Tasks: 新建汇率货币选择器按 code 首字母索引

> 按用户可感知结果拆分；未获 Plan 确认前不实现代码。

## 1. 打开新建汇率即可选「全部」货币

- [x] 1.1 新建汇率打开时，货币选项不再受单页 100 条截断；管理员能看到库中全部货币（通过既有分页 API 拉齐）
- [x] 1.2 拉取过程有明确 loading；失败时有错误提示且不静默成空成功

## 2. 货币按 code 首字母好找

- [x] 2.1 选项按 `code` 首字母分组展示；同组内按完整 `code` 排序
- [x] 2.2 无 `code` 的货币出现在 `#` 分组
- [x] 2.3 选项文案为 `code (name)`（无 code 仅名称），与按 code 排序一致

## 3. 右侧字母索引可跳转

- [x] 3.1 自定义下拉右侧提供字母（及 `#`，在适用时）索引；列表滚动条可隐藏，滚轮仍可用
- [x] 3.2 点击某字母后，列表滚动到对应分组，无需手动长滑

## 4. 选择后仍能正常建汇率

- [x] 4.1 选中货币后写入原有 `currencyId`，提交创建汇率行为与改前一致
- [x] 4.2 编辑汇率、列表筛选等其它入口不因本需求被错误改坏（回归冒烟）

## 5. 验收与文档

- [x] 5.1 对照本 change `specs/rate/spec.md` Scenario 逐条 Verify
- [x] 5.2 按项目惯例补最小相关测试（若有前端测框架覆盖点）
- [x] 5.3 实现完成后回写权威层：本 change tasks 勾选；必要时更新 `openspec/specs/ui.md` 摘要（**不**在未归档前改稳定领域 spec 正文以外的已确认边界）
