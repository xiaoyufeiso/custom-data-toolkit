# UI 交互

## 设计语言

- 形态：后台管理系统
  - **顶栏**：产品名 + 语言切换 +（预留）用户信息区；不放业务菜单
  - **左侧栏**：业务功能入口（货币 / 汇率）；由路由 `meta.menu` 派生
  - **内容区**：当前路由页面
- 登录等 `meta.public` 页：隐藏侧栏，仅保留顶栏
- 组件库：优先 tendata-ui（随 Tendata/公司前端模板）
- 反馈：操作成功/失败 Toast 或等价提示；删除需确认

## 页面流程

1. `/login` → 登录成功 → `/`（或 `/currencies`）
2. 侧栏：货币管理 / 汇率管理 → 对应列表页
3. 列表页 → 新建/编辑抽屉或弹窗 → 提交回列表
4. （搁置）API Key 管理前端：创建成功后一次性展示明文；当前无路由/页面

## 核心页面交互规则

### 登录页
- 用户名、密码；无注册入口
- 错误信息不区分用户是否存在
- 登录页视觉/组件库对齐仍见活跃 change `standardize-admin-ui-components`

### 货币列表
- 分页；支持按名称/code 搜索（`q`）
- 搜索框支持名称/code 前缀推荐（最多 10 条）；选中仅填入，不自动搜索
- 当前页行选择 + 页面顶部批量删除（操作列无删除）；删除有关联汇率时展示服务端错误
- 选择在翻页、改页大小、筛选变化或刷新后清除

### 汇率列表
- 筛选区与批量操作区分区：标题区仅「新建汇率」；「筛选条件」含 code、日期、状态、筛选、重置；「批量操作」含选择计数、批量核对、批量删除
- code 筛选支持 code 前缀推荐；选中仅填入，不自动搜索
- 支持新建/编辑 `data` 与 `checked`
- 批量核对经确认后调用 `POST /rates/batch-check`；已核对幂等
- **新建汇率**货币选择：自定义下拉；选项文案 `code (name)`（无 code 仅名称）；按 `code` 首字母分组；无 code 归 `#`；右侧仅展示有数据的字母索引（见 change `improve-rate-create-currency-picker`，未实现）

### API Key 列表（前端搁置）
- 后端管理 API 仍可用；前端路由/页面/服务已移除（2026-07-31）
- 恢复时需重建 UI，并继续 deferred：筛选、批量删除、BizTable 对齐

## 通用交互规范

- 表单提交后 MUST 显示成功/失败反馈
- 删除与批量破坏性操作 MUST 二次确认
- 列表页 SHOULD 支持分页
- 加载中 MUST 显示 loading
- 未登录访问管理页 MUST 跳转登录
- 服务端业务错误 MUST 展示用户可读文案，不追加错误码

## 响应式要求

- MVP 以桌面后台为主；不强制移动端完整适配

## 可访问性

- SHOULD：关键按钮具备可辨识文本；表单 label 完整
